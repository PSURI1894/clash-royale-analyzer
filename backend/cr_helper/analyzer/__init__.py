"""Deterministic deck analyzer (Phase 1).

Pure, rule-based metrics + archetype + vulnerability assessment. No LLM, no
network — every number is derived from the card database. The LLM-based advisor
(Phase 4) will narrate over these outputs, never recompute them.
"""
